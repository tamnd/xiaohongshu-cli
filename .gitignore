/dist/
/bin/
# A stray root build: `make` puts the binary in bin/, but `go build` at the
# root drops a 5MB Mach-O here that `git add -A` will happily stage.
/xhs
*.out
*.test
.DS_Store

# Built docs site (tago output) and its cache
/docs/public/
/docs/public-pages/
/docs/public-cf/
**/.tago-cache.db
